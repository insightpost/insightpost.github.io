// Generated by Trae Work
(function() {
  'use strict';

  var style = getComputedStyle(document.documentElement);
  var accent = style.getPropertyValue('--accent').trim() || '#c41e3a';
  var accent2 = style.getPropertyValue('--accent2').trim() || '#1e40af';
  var ink = style.getPropertyValue('--ink').trim() || '#1a1a1a';
  var muted = style.getPropertyValue('--muted').trim() || '#6b7280';
  var rule = style.getPropertyValue('--rule').trim() || '#e5e7eb';
  var bg2 = style.getPropertyValue('--bg2').trim() || '#f8fafc';
  var positive = '#16a34a';
  var negative = '#dc2626';

  var palette = [accent, accent2, '#d97706', '#059669', '#7c3aed', '#0891b2', '#be185d', '#65a30d'];

  // ==================== Data ====================

  // 集团五大板块数据（2024年年报口径，2025年细分未完全披露，以2024年板块数据为基础）
  var segmentData = [
    { name: '综合金融服务', revenue: 2794.7, profit: 526.5, color: accent },
    { name: '先进材料', revenue: 3256.2, profit: 103.1, color: accent2 },
    { name: '先进智造', revenue: 507.9, profit: 8.7, color: '#d97706' },
    { name: '新消费', revenue: 498.7, profit: 0.4, color: '#059669' },
    { name: '新型城镇化', revenue: 469.9, profit: 51.4, color: '#7c3aed' }
  ];

  // 主要子公司财务数据（2025年全年，人民币亿元）
  var subsidiaries = [
    { name: '中信银行', segment: '金融', revenue: 2124.75, profit: 706.18, listed: 'A+H', code: '601998/0998' },
    { name: '中信金属', segment: '材料', revenue: 1418.19, profit: 26.89, listed: 'A股', code: '601061' },
    { name: '中信特钢', segment: '智造', revenue: 1073.73, profit: 59.29, listed: 'A股', code: '000708' },
    { name: '白银有色', segment: '材料', revenue: 854.58, profit: -7.55, listed: 'A股', code: '601212' },
    { name: '中信证券', segment: '金融', revenue: 748.54, profit: 300.76, listed: 'A+H', code: '600030/06030' },
    { name: '南钢股份', segment: '智造', revenue: 579.94, profit: 28.67, listed: 'A股', code: '600282' },
    { name: '中信戴卡', segment: '智造', revenue: 490.0, profit: 15.9, listed: '非上市', code: '拟上市' },
    { name: '中信金融资产', segment: '金融', revenue: 804.76, profit: 110.86, listed: 'H股', code: '02799' },
    { name: '中信建投', segment: '金融', revenue: 233.22, profit: 94.39, listed: 'A+H', code: '601066/06066' },
    { name: '中信资源', segment: '材料', revenue: 139.0, profit: 1.6, listed: 'H股', code: '01205' },
    { name: '中信建设', segment: '城镇化', revenue: 167.29, profit: null, listed: '非上市', code: '-' },
    { name: '中信环境', segment: '城镇化', revenue: 100.0, profit: null, listed: '非上市', code: '-' },
    { name: '中信国际电讯', segment: '消费', revenue: 89.0, profit: 8.6, listed: 'H股', code: '01883' },
    { name: '中信重工', segment: '智造', revenue: 81.07, profit: 3.75, listed: 'A股', code: '601608' },
    { name: '隆平高科', segment: '消费', revenue: 84.77, profit: 1.66, listed: 'A股', code: '000998' },
    { name: '南方锰业', segment: '材料', revenue: 59.0, profit: 1.6, listed: 'H股', code: '01091' },
    { name: '国安股份', segment: '城镇化', revenue: 33.23, profit: 0.94, listed: 'A股', code: '000839' },
    { name: '中信海直', segment: '消费', revenue: 22.35, profit: 3.09, listed: 'A股', code: '000099' },
    { name: '中信出版', segment: '消费', revenue: 17.02, profit: 1.30, listed: 'A股', code: '300788' },
    { name: '中信信托', segment: '金融', revenue: 53.79, profit: 26.53, listed: '非上市', code: '-' },
    { name: '中信尼雅', segment: '消费', revenue: 1.23, profit: -0.26, listed: 'A股', code: '600084' }
  ];

  // Sort by revenue descending for ranking charts
  var sortedByRevenue = subsidiaries.slice().sort(function(a, b) { return b.revenue - a.revenue; });
  var sortedByProfit = subsidiaries.filter(function(s) { return s.profit !== null; }).slice().sort(function(a, b) { return b.profit - a.profit; });

  // ==================== Chart 1: 集团五大板块营收结构 ====================
  var el1 = document.getElementById('chart-segment-revenue');
  if (el1) {
    var chart1 = echarts.init(el1, null, { renderer: 'svg' });
    chart1.setOption({
      animation: false,
      tooltip: {
        trigger: 'item',
        appendToBody: true,
        formatter: function(p) {
          return p.name + '<br/>营收: ' + p.value.toLocaleString() + ' 亿元 (' + p.percent + '%)';
        }
      },
      legend: {
        orient: 'vertical',
        right: 20,
        top: 'center',
        textStyle: { color: muted, fontSize: 12 }
      },
      series: [{
        type: 'pie',
        radius: ['45%', '70%'],
        center: ['35%', '50%'],
        avoidLabelOverlap: false,
        itemStyle: {
          borderRadius: 4,
          borderColor: '#fff',
          borderWidth: 2
        },
        label: {
          show: true,
          position: 'outside',
          formatter: '{b}\n{d}%',
          color: ink,
          fontSize: 11
        },
        labelLine: { show: true, length: 10, length2: 10 },
        data: segmentData.map(function(d) {
          return { name: d.name, value: d.revenue, itemStyle: { color: d.color } };
        })
      }]
    });
    window.addEventListener('resize', function() { chart1.resize(); });
  }

  // ==================== Chart 2: 集团五大板块净利润结构 ====================
  var el2 = document.getElementById('chart-segment-profit');
  if (el2) {
    var chart2 = echarts.init(el2, null, { renderer: 'svg' });
    chart2.setOption({
      animation: false,
      tooltip: {
        trigger: 'item',
        appendToBody: true,
        formatter: function(p) {
          return p.name + '<br/>净利润: ' + p.value.toLocaleString() + ' 亿元 (' + p.percent + '%)';
        }
      },
      legend: {
        orient: 'vertical',
        right: 20,
        top: 'center',
        textStyle: { color: muted, fontSize: 12 }
      },
      series: [{
        type: 'pie',
        radius: ['45%', '70%'],
        center: ['35%', '50%'],
        avoidLabelOverlap: false,
        itemStyle: {
          borderRadius: 4,
          borderColor: '#fff',
          borderWidth: 2
        },
        label: {
          show: true,
          position: 'outside',
          formatter: '{b}\n{d}%',
          color: ink,
          fontSize: 11
        },
        labelLine: { show: true, length: 10, length2: 10 },
        data: segmentData.map(function(d) {
          return { name: d.name, value: Math.max(d.profit, 0.1), itemStyle: { color: d.color } };
        })
      }]
    });
    window.addEventListener('resize', function() { chart2.resize(); });
  }

  // ==================== Chart 3: 主要子公司营收排名 (Top 15) ====================
  var el3 = document.getElementById('chart-revenue-rank');
  if (el3) {
    var chart3 = echarts.init(el3, null, { renderer: 'svg' });
    var top15Rev = sortedByRevenue.slice(0, 15).reverse();
    chart3.setOption({
      animation: false,
      tooltip: {
        trigger: 'axis',
        axisPointer: { type: 'shadow' },
        appendToBody: true,
        formatter: function(params) {
          var p = params[0];
          var item = top15Rev[p.dataIndex];
          var profitStr = item.profit !== null ? item.profit.toFixed(2) + ' 亿元' : '未披露';
          return '<strong>' + item.name + '</strong><br/>' +
            '板块: ' + item.segment + '<br/>' +
            '上市: ' + item.listed + ' ' + item.code + '<br/>' +
            '营业收入: <strong>' + p.value.toLocaleString() + '</strong> 亿元<br/>' +
            '净利润: ' + profitStr;
        }
      },
      grid: { left: 110, right: 80, top: 20, bottom: 30 },
      xAxis: {
        type: 'value',
        axisLabel: { color: muted, fontSize: 11, fontWeight: 500, formatter: function(v) { return v.toLocaleString(); } },
        splitLine: { lineStyle: { color: rule, type: 'dashed' } },
        axisLine: { show: false },
        axisTick: { show: false }
      },
      yAxis: {
        type: 'category',
        data: top15Rev.map(function(d) { return d.name; }),
        axisLabel: { color: ink, fontSize: 12, fontWeight: 500 },
        axisLine: { lineStyle: { color: rule } },
        axisTick: { show: false }
      },
      series: [{
        type: 'bar',
        data: top15Rev.map(function(d, i) {
          var segColors = { '金融': accent, '材料': accent2, '智造': '#d97706', '消费': '#059669', '城镇化': '#7c3aed' };
          return {
            value: d.revenue,
            itemStyle: { color: segColors[d.segment] || palette[i % palette.length], borderRadius: [0, 4, 4, 0] }
          };
        }),
        barWidth: 20,
        label: {
          show: true,
          position: 'right',
          formatter: function(p) { return p.value.toLocaleString(); },
          color: ink,
          fontSize: 12,
          fontWeight: 700,
          distance: 6
        }
      }]
    });
    window.addEventListener('resize', function() { chart3.resize(); });
  }

  // ==================== Chart 4: 主要子公司净利润排名 ====================
  var el4 = document.getElementById('chart-profit-rank');
  if (el4) {
    var chart4 = echarts.init(el4, null, { renderer: 'svg' });
    var top15Profit = sortedByProfit.slice(0, 15).reverse();
    // Calculate x-axis range - all bars extend rightward, use absolute value for bar length
    var maxAbsProfit = Math.max.apply(null, top15Profit.map(function(d) { return Math.abs(d.profit); }));
    var xMax = Math.ceil(maxAbsProfit * 1.2);
    chart4.setOption({
      animation: false,
      tooltip: {
        trigger: 'axis',
        axisPointer: { type: 'shadow' },
        appendToBody: true,
        formatter: function(params) {
          var p = params[0];
          var item = top15Profit[p.dataIndex];
          var profitVal = item.profit;
          return '<strong>' + item.name + '</strong><br/>' +
            '净利润: <strong style="color:' + (profitVal >= 0 ? positive : negative) + '">' + profitVal.toFixed(2) + '</strong> 亿元<br/>' +
            '营业收入: ' + item.revenue.toLocaleString() + ' 亿元';
        }
      },
      grid: { left: 110, right: 90, top: 20, bottom: 30 },
      xAxis: {
        type: 'value',
        min: 0,
        max: xMax,
        axisLabel: {
          color: muted,
          fontSize: 11,
          fontWeight: 500,
          formatter: function(v) { return v.toLocaleString(); }
        },
        splitLine: { lineStyle: { color: rule, type: 'dashed' } },
        axisLine: { show: false },
        axisTick: { show: false }
      },
      yAxis: {
        type: 'category',
        data: top15Profit.map(function(d) { return d.name; }),
        axisLabel: { color: ink, fontSize: 12, fontWeight: 500 },
        axisLine: { lineStyle: { color: rule } },
        axisTick: { show: false }
      },
      series: [{
        type: 'bar',
        data: top15Profit.map(function(d) {
          var isNeg = d.profit < 0;
          return {
            value: Math.abs(d.profit),
            actualValue: d.profit,
            itemStyle: {
              color: isNeg ? negative : accent,
              borderRadius: [0, 4, 4, 0]
            }
          };
        }),
        barWidth: 20,
        label: {
          show: true,
          position: 'right',
          formatter: function(p) {
            return p.data.actualValue.toFixed(1);
          },
          color: function(p) { return p.data.actualValue >= 0 ? ink : negative; },
          fontSize: 12,
          fontWeight: 700,
          distance: 8
        }
      }]
    });
    window.addEventListener('resize', function() { chart4.resize(); });
  }

  // ==================== Chart 5: 金融板块子公司对比 ====================
  var el5 = document.getElementById('chart-finance');
  if (el5) {
    var chart5 = echarts.init(el5, null, { renderer: 'svg' });
    var finCos = subsidiaries.filter(function(s) { return s.segment === '金融' && s.profit !== null; })
      .sort(function(a, b) { return b.revenue - a.revenue; });
    chart5.setOption({
      animation: false,
      tooltip: {
        trigger: 'axis',
        axisPointer: { type: 'shadow' },
        appendToBody: true
      },
      legend: {
        data: ['营业收入', '净利润'],
        top: 0,
        textStyle: { color: muted, fontSize: 12 }
      },
      grid: { left: 80, right: 50, top: 50, bottom: 30 },
      xAxis: {
        type: 'category',
        data: finCos.map(function(d) { return d.name; }),
        axisLabel: { color: ink, fontSize: 11, rotate: 20 },
        axisLine: { lineStyle: { color: rule } },
        axisTick: { show: false }
      },
      yAxis: [
        {
          type: 'value',
          name: '营业收入(亿元)',
          nameTextStyle: { color: muted, fontSize: 11 },
          axisLabel: { color: muted },
          splitLine: { lineStyle: { color: rule, type: 'dashed' } },
          axisLine: { show: false },
          axisTick: { show: false }
        },
        {
          type: 'value',
          name: '净利润(亿元)',
          nameTextStyle: { color: muted, fontSize: 11 },
          axisLabel: { color: muted },
          splitLine: { show: false },
          axisLine: { show: false },
          axisTick: { show: false }
        }
      ],
      series: [
        {
          name: '营业收入',
          type: 'bar',
          data: finCos.map(function(d) { return d.revenue; }),
          itemStyle: { color: accent, borderRadius: [4, 4, 0, 0] },
          barWidth: 28
        },
        {
          name: '净利润',
          type: 'bar',
          yAxisIndex: 1,
          data: finCos.map(function(d) { return d.profit; }),
          itemStyle: { color: accent2, borderRadius: [4, 4, 0, 0] },
          barWidth: 28
        }
      ]
    });
    window.addEventListener('resize', function() { chart5.resize(); });
  }

  // ==================== Chart 6: 净利率对比散点图 ====================
  var el6 = document.getElementById('chart-margin');
  if (el6) {
    var chart6 = echarts.init(el6, null, { renderer: 'svg' });
    var marginData = subsidiaries.filter(function(s) {
      return s.profit !== null && s.revenue >= 10;
    }).map(function(s) {
      var margin = (s.profit / s.revenue) * 100;
      var segColors = { '金融': accent, '材料': accent2, '智造': '#d97706', '消费': '#059669', '城镇化': '#7c3aed' };
      return {
        name: s.name,
        value: [s.revenue, margin, s.profit],
        itemStyle: { color: segColors[s.segment] }
      };
    });

    chart6.setOption({
      animation: false,
      tooltip: {
        trigger: 'item',
        appendToBody: true,
        formatter: function(p) {
          return '<strong>' + p.name + '</strong><br/>' +
            '营收: ' + p.value[0].toLocaleString() + ' 亿元<br/>' +
            '净利率: <strong>' + p.value[1].toFixed(2) + '%</strong><br/>' +
            '净利润: ' + p.value[2].toFixed(2) + ' 亿元';
        }
      },
      legend: {
        data: [
          { name: '综合金融', itemStyle: { color: accent } },
          { name: '先进材料', itemStyle: { color: accent2 } },
          { name: '先进智造', itemStyle: { color: '#d97706' } },
          { name: '新消费', itemStyle: { color: '#059669' } },
          { name: '新型城镇化', itemStyle: { color: '#7c3aed' } }
        ],
        top: 0,
        textStyle: { color: muted, fontSize: 11 }
      },
      grid: { left: 70, right: 30, top: 50, bottom: 50 },
      xAxis: {
        type: 'log',
        name: '营业收入（亿元，对数刻度）',
        nameLocation: 'middle',
        nameGap: 30,
        nameTextStyle: { color: muted, fontSize: 11 },
        axisLabel: { color: muted },
        splitLine: { lineStyle: { color: rule, type: 'dashed' } },
        axisLine: { lineStyle: { color: rule } },
        axisTick: { show: false }
      },
      yAxis: {
        type: 'value',
        name: '净利率（%）',
        nameTextStyle: { color: muted, fontSize: 11 },
        axisLabel: { color: muted, formatter: '{value}%' },
        splitLine: { lineStyle: { color: rule, type: 'dashed' } },
        axisLine: { lineStyle: { color: rule } },
        axisTick: { show: false }
      },
      series: [{
        type: 'scatter',
        data: marginData,
        symbolSize: function(d) {
          return Math.max(Math.sqrt(Math.abs(d[2])) * 2.5, 8);
        },
        label: {
          show: true,
          formatter: function(p) { return p.name; },
          position: 'top',
          color: ink,
          fontSize: 10
        },
        emphasis: {
          itemStyle: {
            shadowBlur: 10,
            shadowColor: 'rgba(0,0,0,0.2)'
          }
        }
      }]
    });
    window.addEventListener('resize', function() { chart6.resize(); });
  }

  // ==================== Chart 7: 实业板块子公司对比 ====================
  var el7 = document.getElementById('chart-industrial');
  if (el7) {
    var chart7 = echarts.init(el7, null, { renderer: 'svg' });
    var indCos = subsidiaries.filter(function(s) {
      return s.segment !== '金融' && s.profit !== null && s.revenue >= 10;
    }).sort(function(a, b) { return b.revenue - a.revenue; });

    chart7.setOption({
      animation: false,
      tooltip: {
        trigger: 'axis',
        axisPointer: { type: 'shadow' },
        appendToBody: true
      },
      legend: {
        data: ['营业收入', '净利润'],
        top: 0,
        textStyle: { color: muted, fontSize: 12 }
      },
      grid: { left: 100, right: 50, top: 50, bottom: 80 },
      xAxis: {
        type: 'value',
        axisLabel: { color: muted, formatter: '{value}亿' },
        splitLine: { lineStyle: { color: rule, type: 'dashed' } },
        axisLine: { show: false },
        axisTick: { show: false }
      },
      yAxis: {
        type: 'category',
        data: indCos.map(function(d) { return d.name; }),
        axisLabel: { color: ink, fontSize: 11 },
        axisLine: { lineStyle: { color: rule } },
        axisTick: { show: false }
      },
      series: [
        {
          name: '营业收入',
          type: 'bar',
          data: indCos.map(function(d) { return d.revenue; }),
          itemStyle: { color: accent2, borderRadius: [0, 4, 4, 0] },
          barWidth: 14,
          label: {
            show: true,
            position: 'right',
            formatter: function(p) { return p.value.toLocaleString(); },
            color: muted,
            fontSize: 10
          }
        }
      ]
    });
    window.addEventListener('resize', function() { chart7.resize(); });
  }

  // ==================== Chart 8: 板块汇总对比柱状图 ====================
  var el8 = document.getElementById('chart-segment-compare');
  if (el8) {
    var chart8 = echarts.init(el8, null, { renderer: 'svg' });
    chart8.setOption({
      animation: false,
      tooltip: {
        trigger: 'axis',
        axisPointer: { type: 'shadow' },
        appendToBody: true,
        formatter: function(params) {
          var rev = params[0];
          var prof = params[1];
          var margin = rev.value > 0 ? (prof.value / rev.value * 100).toFixed(1) : 'N/A';
          return '<strong>' + rev.name + '</strong><br/>' +
            '营业收入: ' + rev.value.toLocaleString() + ' 亿元<br/>' +
            '净利润: ' + prof.value.toFixed(1) + ' 亿元<br/>' +
            '板块净利率: ' + margin + '%';
        }
      },
      legend: {
        data: ['营业收入', '净利润'],
        top: 0,
        textStyle: { color: muted, fontSize: 12 }
      },
      grid: { left: 80, right: 60, top: 50, bottom: 30 },
      xAxis: {
        type: 'category',
        data: segmentData.map(function(d) { return d.name; }),
        axisLabel: { color: ink, fontSize: 11, interval: 0 },
        axisLine: { lineStyle: { color: rule } },
        axisTick: { show: false }
      },
      yAxis: [
        {
          type: 'value',
          name: '营收(亿元)',
          nameTextStyle: { color: muted, fontSize: 11 },
          axisLabel: { color: muted },
          splitLine: { lineStyle: { color: rule, type: 'dashed' } },
          axisLine: { show: false },
          axisTick: { show: false }
        },
        {
          type: 'value',
          name: '净利润(亿元)',
          nameTextStyle: { color: muted, fontSize: 11 },
          axisLabel: { color: muted },
          splitLine: { show: false },
          axisLine: { show: false },
          axisTick: { show: false }
        }
      ],
      series: [
        {
          name: '营业收入',
          type: 'bar',
          data: segmentData.map(function(d) { return d.revenue; }),
          itemStyle: { color: accent + 'cc', borderRadius: [4, 4, 0, 0] },
          barWidth: 40
        },
        {
          name: '净利润',
          type: 'bar',
          yAxisIndex: 1,
          data: segmentData.map(function(d) { return d.profit; }),
          itemStyle: { color: accent2, borderRadius: [4, 4, 0, 0] },
          barWidth: 40
        }
      ]
    });
    window.addEventListener('resize', function() { chart8.resize(); });
  }

})();
